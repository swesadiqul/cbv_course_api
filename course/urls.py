from django.urls import path
# from .views import *

urlpatterns = [
    # path('course', CourseAPIView.as_view(), name="course_api"),
    # path('course-details/<int:pk>', CourseAPIDetail.as_view(), name="course_api"),
]
